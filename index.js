// const input = document.getElementById('input');

// function clickAlert() {
//   alert('I was clicked!');
// }

// input.addEventListener('click', clickAlert);
function addingEventListener(addEventlistener){
    addEventlistener("click", addEventlistener);
    if (addEventlistener === click){

    }else(addingEventListener = addEventlistener)
    return addEventlistener

}
addingEventListener()
